import PlaceholderPage from "../../components/PlaceholderPage";

function OnlinePrediction() {
  return (
    <PlaceholderPage
      title="在线预测"
      description="用于提交样本并返回分类预测结果（占位页面）。"
    />
  );
}

export default OnlinePrediction;
