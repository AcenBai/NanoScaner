import PlaceholderPage from "../../components/PlaceholderPage";

function PreprocessSignal() {
  return (
    <PlaceholderPage
      title="信号预处理"
      description="用于执行信号去噪、归一化、分段等流程（占位页面）。"
    />
  );
}

export default PreprocessSignal;
